import React from "react";

// Functional component that receives props
function Greeting({ name }) {
    // Displays a greeting message using the name prop
    return <h1>Hello, {name}!</h1>;
  }
  
  export default Greeting;
  